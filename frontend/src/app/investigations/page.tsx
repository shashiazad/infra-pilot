import Link from "next/link";
import { SeverityBadge, StatusBadge, relativeTime } from "@/components/operations-ui";
import { apiGet } from "@/lib/api";
import type { InvestigationListItem } from "@/types";

export default async function InvestigationsPage(){let rows:InvestigationListItem[]=[];let error:string|null=null;try{rows=await apiGet<InvestigationListItem[]>("/investigations")}catch(caught){error=caught instanceof Error?caught.message:"Request failed"}return <main className="page"><div className="page-header"><div><span className="eyebrow">Agent execution</span><h1>Investigations</h1><p>Evidence collection, analysis, and remediation proposals.</p></div></div>{error?<div className="empty-state"><strong>Investigation feed unavailable</strong><p>{error}</p><div className="technical-note">GET /api/v1/investigations</div></div>:rows.length===0?<div className="empty-state"><strong>No investigations yet</strong><p>Open an incident and run the agent to create the first investigation.</p><Link className="text-link" href="/incidents">Open incidents</Link></div>:<section className="panel"><div className="panel-header"><h2>Agent runs</h2><span className="panel-subtitle">{rows.length} records</span></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Run</th><th>Incident</th><th>Severity</th><th>Service</th><th>Status</th><th>Confidence</th><th>Started</th></tr></thead><tbody>{rows.map(run=><tr key={run.run_id}><td><Link className="text-link mono" href={`/investigations/${run.run_id}`}>{run.run_id.slice(0,8)}</Link></td><td className="primary-cell">{run.incident_title}</td><td><SeverityBadge value={run.severity}/></td><td className="mono">{run.service}</td><td><StatusBadge value={run.status}/></td><td className="mono">{run.confidence===null?"—":`${Math.round(run.confidence*100)}%`}</td><td className="relative-time">{relativeTime(run.started_at)}</td></tr>)}</tbody></table></div></section>}</main>}
