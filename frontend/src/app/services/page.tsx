import { StatusBadge } from "@/components/operations-ui";
import { apiGet } from "@/lib/api";
import type { ServiceSnapshot } from "@/types";

function formatMetric(value:number|null,suffix:string){return value===null?"—":`${value.toFixed(2)}${suffix}`}
export default async function ServicesPage(){let services:ServiceSnapshot[]=[];let error:string|null=null;try{services=await apiGet<ServiceSnapshot[]>("/services?namespace=prod-demo")}catch(caught){error=caught instanceof Error?caught.message:"Request failed"}return <main className="page"><div className="page-header"><div><span className="eyebrow">Infrastructure inventory</span><h1>Services</h1><p>Live Kubernetes health, deployment state, resource usage, and Prometheus signals.</p></div></div>{error?<div className="empty-state"><strong>Service inventory unavailable</strong><p>{error}</p><div className="technical-note">Verify kind-prod-demo-cluster, metrics-server, and the backend API.</div></div>:services.length===0?<div className="empty-state"><strong>No deployments found</strong><p>No Kubernetes deployments exist in namespace <span className="mono">prod-demo</span>.</p></div>:<section className="panel"><div className="panel-header"><h2>Live service inventory</h2><span className="panel-subtitle">namespace: prod-demo</span></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Service</th><th>Health</th><th>Ready pods</th><th>CPU</th><th>Memory</th><th>Restarts</th><th>5xx</th><th>p95</th><th>Warnings</th></tr></thead><tbody>{services.map(service=><tr key={service.service}><td><span className="primary-cell mono">{service.service}</span><span className="subtext">{service.deployment_status}</span></td><td><StatusBadge value={service.health}/></td><td className="mono">{service.ready_replicas}/{service.desired_replicas}</td><td className="mono">{service.cpu??"—"}</td><td className="mono">{service.memory??"—"}</td><td className="mono">{service.restarts}</td><td className="mono">{formatMetric(service.http_5xx_rate_percent,"%")}</td><td className="mono">{formatMetric(service.p95_latency_ms," ms")}</td><td>{service.recent_warnings.length}</td></tr>)}</tbody></table></div></section>}</main>}
