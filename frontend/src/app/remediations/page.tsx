import Link from "next/link";
import { StatusBadge, relativeTime } from "@/components/operations-ui";
import { apiGet } from "@/lib/api";
import type { RemediationAuditItem } from "@/types";

export default async function RemediationsPage(){let rows:RemediationAuditItem[]=[];let error:string|null=null;try{rows=await apiGet<RemediationAuditItem[]>("/investigations/remediations/audit")}catch(caught){error=caught instanceof Error?caught.message:"Request failed"}return <main className="page"><div className="page-header"><div><span className="eyebrow">Human control plane</span><h1>Remediations</h1><p>Approval decisions, allow-listed execution, and auditable outcomes.</p></div></div>{error?<div className="empty-state"><strong>Remediation audit unavailable</strong><p>{error}</p><div className="technical-note">GET /api/v1/investigations/remediations/audit</div></div>:rows.length===0?<div className="empty-state"><strong>No remediation proposals</strong><p>Proposals will appear after an investigation identifies an allow-listed action.</p></div>:<section className="panel"><div className="panel-header"><h2>Remediation audit</h2><span className="panel-subtitle">{rows.length} proposals</span></div><div className="table-wrap"><table className="data-table"><thead><tr><th>Time</th><th>Incident</th><th>Action</th><th>Target</th><th>Risk</th><th>Approval</th><th>Execution</th><th>Result</th></tr></thead><tbody>{rows.map(row=><tr key={row.run_id}><td className="relative-time">{relativeTime(row.time)}</td><td><Link className="text-link primary-cell" href={`/investigations/${row.run_id}`}>{row.incident_title}</Link><span className="subtext mono">{row.incident_id.slice(0,8)}</span></td><td className="mono">{row.action}</td><td className="mono">{row.target}</td><td><StatusBadge value={row.risk}/></td><td><StatusBadge value={row.approval_status??"PENDING"}/></td><td><StatusBadge value={row.remediation_status??"NOT STARTED"}/></td><td>{row.result?String(row.result.success??"Recorded"):"—"}</td></tr>)}</tbody></table></div></section>}</main>}
